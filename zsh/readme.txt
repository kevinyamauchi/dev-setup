this .zshrc file sets up autocomplete including from the command history.

it additionally adds the oh my posh initialialization. the path to the config will have to be updated.

finally, it adds some helpful aliases
